= render :partial => 'menu'
div.scripts-leaderboard
  h2 Leaderboard Inventories

  div.leaderboard
    div.row
      div.span2 Name
      div.span2 Total Value
      div.span2 BTC
      div.span2 USD
    - @script_values.each do |sv|
      div.row
        div.span2
          span.name
            a href="/#{sv[:script].user.slug}/#{sv[:script].slug}"
              | #{sv[:script].user.slug}/#{sv[:script].slug}
        div.span2
          span.invamt
            | #{"%0.5f" % sv[:value]}BTC
        div.span2
          span.invamt
            | #{"%0.5f" % sv[:script].inventory['btc']}BTC
        div.span2
          span.invamt
            | #{"%0.5f"% (sv[:script].inventory['usd'] || 0)}USD

  div.value
    | USD inventory valued by last mtgox price of $#{@rates["BTCUSD"]}